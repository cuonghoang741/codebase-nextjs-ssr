version: "3.3"

services:
  app:
    container_name: vivivi-app-server
    build:
      context: ./
      dockerfile: ./Dockerfile
    working_dir: /home/node/app
#    command: [ "npm", "run", "start:dev" ]
    restart: unless-stopped
    tty: true
    env_file:
      - .env
    volumes:
      - ./:/home/node/app
    ports:
      - ${PORT}:${PORT}
    networks:
      - vivivi-app-network
    depends_on:
      - redis

    # If you want to use local database, use this code
  postgres:
    container_name: vivivi-postgres
    image: postgres:13.5
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - ${DB_PORT}:5432
    networks:
      - vivivi-app-network

  redis:
    container_name: vivivi-app-redis
    image: redis/redis-stack-server:latest
    restart: unless-stopped
    ports:
      - ${REDIS_PORT}:${REDIS_PORT}
    volumes:
      - redis:/data
    networks:
      - vivivi-app-network

volumes:
  redis:
    driver: local
  postgres:
    driver: local # local, overlay

networks:
  vivivi-app-network:
    driver: bridge